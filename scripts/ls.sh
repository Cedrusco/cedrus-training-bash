#!/bin/bash

# Print current directory
ls

# Print all files in a directory matching 'bash'
ls /usr/bin/*bash*

# Print current directory with hidden files
ls -a

# Print current directory with file permissions
ls -ld *